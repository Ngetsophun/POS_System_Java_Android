<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.POS_System" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryVariant">@color/blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/blue</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="MyTextfield" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="boxBackgroundColor">@color/white</item>
        <item name="boxCornerRadiusBottomEnd">20dp</item>
    </style>
    <style name="BottomSheetStyle" parent="Theme.Design.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/Model</item>
    </style>
    <style name="Model" parent="Widget.Design.BottomSheet.Modal">
        <item name= "background">@layout/bottom_sheet_sale_dialog</item>
    </style>


    <style name="toolbartext" parent="TextAppearance.MaterialComponents.Button">

    </style>
    <style name="ViewProduct.Toobar" parent="Widget.AppCompat.Toolbar">
        <item name="background">@color/blue</item>
       <item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>

        <item name="titleTextAppearance">@style/toolbartext</item>
    </style>
</resources>